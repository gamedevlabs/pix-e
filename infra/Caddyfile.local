localhost {
    handle /api/* {
        reverse_proxy backend-dev:{$DJANGO_PORT_DEV} {
            flush_interval -1
        }
    }

    handle {
        encode gzip
        reverse_proxy frontend-dev:3000
    }
}